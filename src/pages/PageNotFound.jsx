import React from 'react';
import Layout from '../components/layout/Layout';
import PageNotFoundContent from './users/PageNotFoundContent';

const PageNotFound = () => {
  return (
    <Layout>
      <PageNotFoundContent />
    </Layout>
  );
};

export default PageNotFound;
